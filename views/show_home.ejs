<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <title>ショーシフト管理システム</title>
    </head>
    <body>
        
        <script src = "../public/js/make_header.js" type="text/javascript"></script>
        <main>
            
            <div class="title">
                <h2><%= show_name %></h2>
            </div>
            <div>
                <p><a href= <%= "http://localhost:3000/" + show_id + "/report" %>> </a></p>
            </div>
            <div class="main">
                <h3 id="calendar_title"></h3>
                <table border="2">
                    <thead>
                        <tr id="calendar">
                            <th> 役/日</th>
                            <!-- 日付生成 -->
                             <% for(var key in shift){ %>
                                    <th style="writing-mode: vertical-rl;"> <%= key %> </th>
                             <%};%>
                             
                        </tr>
                    </thead>
                    <tbody>
                        <!-- 一行目はthでヘッダー扱いにして役名一覧 -->
                        <% for(var i = 0; i< rolls.length ; i++ ) {%>
                            <tr>
                                <th>
                                    <%= rolls[i] %>
                                </th>
                                <!-- ここにシフトの内容を入れる -->
                                 <% for(var key in shift){ %>
                                    <td> <%= shift[key][rolls[i]] %></td>
                                <%};%>
                            </tr>
                        <% }; %>
                    </tbody>

            </div>
            <script src="../public/js/make_show_calendar.js" type="text/javascript"></script>
        </main>
   </body>
</html>